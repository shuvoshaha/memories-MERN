import { makeStyles } from "@material-ui/core";

export default makeStyles({
    appBar: {
        borderRadius: 4,
        color: 'red',
        marginBottom: '2rem'
    },
    header: {
        borderRadius: 4
    }
})