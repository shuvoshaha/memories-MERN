import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
    appBar: {
        borderRadius: 4,
        color: 'red',
        marginBottom: '2rem'
    },
    header: {
        borderRadius: 4
    }
})